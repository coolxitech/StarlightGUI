<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="StarlightGUI.ModifyTokenDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StarlightGUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:essential="using:WinUI3Package" 
    mc:Ignorable="d"
    Title="修改进程令牌"
    PrimaryButtonText="修改"
    SecondaryButtonText="取消"
    DefaultButton="Primary"
    PrimaryButtonClick="OnPrimaryButtonClick"    
    Style="{StaticResource DefaultContentDialogStyle}"
    RequestedTheme="Dark">

    <ContentDialog.TitleTemplate>
        <DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <FontIcon Glyph="&#xe70f;" FontSize="30" FontFamily="Segoe Fluent Icons" Margin="0,5,0,0"/>
                <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
            </StackPanel>
        </DataTemplate>
    </ContentDialog.TitleTemplate>

    <Grid>
        <StackPanel Margin="0,0,0,10">
            <TextBlock Text="Starlight GUI 将修改指定进程的令牌至选定权限类型。" 
                       Margin="0,0,0,10"/>

            <ComboBox x:Name="TokenComboBox" 
                      Text="令牌"
                      SelectedIndex="0"
                      essential:ComboBoxHelper.AcrylicWorkaround="True">
                <ComboBoxItem Content="System" />
                <ComboBoxItem Content="TrustedInstaller" />
                <ComboBoxItem Content="DWM" />
            </ComboBox>
        </StackPanel>
    </Grid>
</ContentDialog>