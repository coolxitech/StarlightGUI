<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="StarlightGUI.CopyFileDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StarlightGUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:essential="using:WinUI3Package" 
    mc:Ignorable="d"
    Title="复制文件"
    PrimaryButtonText="复制"
    SecondaryButtonText="取消"
    DefaultButton="Primary"
    PrimaryButtonClick="OnPrimaryButtonClick"    
    Style="{StaticResource DefaultContentDialogStyle}"
    RequestedTheme="Dark">

    <ContentDialog.TitleTemplate>
        <DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <FontIcon Glyph="&#xe8c8;" FontSize="30" FontFamily="Segoe Fluent Icons" Margin="0,5,0,0"/>
                <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
            </StackPanel>
        </DataTemplate>
    </ContentDialog.TitleTemplate>

    <Grid>
        <!-- 输入框 -->
        <StackPanel Margin="0,0,0,10">
            <TextBlock Text="Starlight GUI 将会把选定的文件/文件夹复制至指定路径。" 
                       Margin="0,0,0,20"/>
            <TextBox x:Name="CopyPathTextBox" 
                     PlaceholderText="文件路径..."
                     HorizontalAlignment="Stretch" Margin="0,0,0,10"/>
            <Button Content="浏览..." Click="ExploreButton_Click"/>
        </StackPanel>
    </Grid>
</ContentDialog>